# 모든 텍스트 파일은 저장소에 LF로 저장 (자동 감지)
* text=auto eol=lf

# 셸/파이썬 스크립트는 항상 LF (Windows 체크아웃에서도 유지)
*.sh   text eol=lf
*.py   text eol=lf

# 바이너리로 취급할 확장자 (줄바꿈 변환 금지)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.pdf  binary
*.ico  binary
